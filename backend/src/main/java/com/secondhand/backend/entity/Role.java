package com.secondhand.backend.entity;

/**
 * User role within the system
 * USER  : regular user ( creates listings , chats , rates sellers , etc )
 * ADMIN : system administrator ( reviews listings , manages users , etc )
 */

public enum Role {
    USER,
    ADMIN
}
